/** Placeholder while `AppShellHeaderNav` streams in — keeps header layout stable. */
export function AppHeaderNavSkeleton() {
  return (
    <div
      className="size-10 shrink-0 animate-pulse rounded-full bg-muted ring-1 ring-border/70"
      aria-hidden
    />
  );
}
